import React from 'react';
import ReactDOM from 'react-dom/client';
import SupportApp from './support-app.jsx';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SupportApp />
  </React.StrictMode>
);