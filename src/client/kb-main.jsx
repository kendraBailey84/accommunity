import React from 'react';
import ReactDOM from 'react-dom/client';
import KnowledgeBaseApp from './kb-app.jsx';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <KnowledgeBaseApp />
  </React.StrictMode>
);